# סקיצה + מידות (פשוט)

כלי נפרד מ־Yarhi Pro.

1. העלאת סקיצה  
2. לחיצה על שני קצוות → הזנת מידה  
3. מילוי אורך קיר + עומק → חשב  

```bash
cd standalone/pergola-sketch-quote
npm install
npm run dev
```
